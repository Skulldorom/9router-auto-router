{
  "name": "9router-auto-router",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic pre-generation routing overlay for 9Router combos",
  "type": "module",
  "scripts": {
    "lint": "eslint eslint.config.js src patches test --max-warnings=0",
    "syntax": "node --check src/auto-router.cjs && node --check patches/apply-patch.mjs && node --check eslint.config.js && node --check test/*.mjs",
    "check": "npm run syntax && npm run lint",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "acorn": "^8.18.0",
    "eslint": "^9.39.5"
  }
}
