{
  "name": "9router-auto-router",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic pre-generation routing overlay for 9Router combos",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/auto-router.cjs && node --check patches/apply-patch.mjs && node --check test/*.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
